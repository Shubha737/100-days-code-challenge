# Day 8 code 2

list = []

number_of_element = int(input("Enter the number of element in the list :"))

for i in range(number_of_element) :
    element = int(input("Enter the elements :"))
    list.append(element)

print(list)
